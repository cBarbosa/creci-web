import React from 'react';

export interface ICustomerPageProps {};

const CustomerPage: React.FunctionComponent<ICustomerPageProps> = (props) => {
    return <div>Cadastro Cliente</div>;
};

export default CustomerPage;
