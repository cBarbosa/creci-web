import React from 'react';

export interface IActivityPageProps {};

const ActivityPage: React.FunctionComponent<IActivityPageProps> = (props) => {
    return(
        <h1>Activity</h1>
    );
};

export default ActivityPage;
